# [START workflows_args]
main:
    params: [args]
    steps:
      - step1:
          assign:
            - outputVar: ${"Hello " + args.firstName + " " + args.lastName}
      - step2:
          return: ${outputVar}
# [END workflows_args]